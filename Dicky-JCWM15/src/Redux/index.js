const INITIAL_STATE = {
    count: 0,
    bool: false,
  };
 
 
  const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export {
    reducer,
  };